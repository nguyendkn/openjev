---
loop: 3
candidate: A_3
status: passed
builder: Developer
assessor: QATester
verified_behaviors:
  - "VR1 (external /health): `curl -s -o /dev/null -w \"%{http_code}\" http://103.146.166.46:80/health` run from this Windows machine's own Bash tool (NOT via SSH) -> 200"
  - "VR2 (external /bench, non-France prompt): POST http://103.146.166.46:80/bench from this Windows machine with the email-routing scenario (researcher-05-benchmark-usecases.md #1: refund/double-charge email, options billing/technical-support/account-access/spam) -> HTTP 200, valid BenchReport JSON, all 7 Timings fields present (model_load_ms, warmup_ms, tokenize_ms, constrained_readout_ms, generation_ms, laya_model_load_ms, laya_inference_ms), readout.probs sums to ~1 (billing=0.9998882 dominant), readout.best_option=billing, generate.parsed.answer=billing, generate.valid=true — correct, sensible classification"
  - "VR3 (concurrency): fired 2 near-simultaneous POST /bench from this machine via bash `&`/`wait` (scenario 5 incident-severity + scenario 6 content-moderation, both from researcher-05-benchmark-usecases.md) -> both returned HTTP 200, each got its OWN correct/distinct result (A: sev-scenario best_option=sev1/generate=sev4; B: moderation-scenario best_option=flag-for-review/generate=flag-for-review) — no cross-contamination, no crash, single worker thread fully serialized both jobs"
  - "VR4 (invalid model, no panic): POST /bench with model=\"nonexistent-model-xyz\" -> HTTP 400, JSON body `{\"error\":\"unknown model: nonexistent-model-xyz\"}}`, not 500/panic; immediately re-curled /health from this machine afterward -> 200, server alive and unaffected"
  - "Code review, apps/server/src/app.rs: `engine.reset_context()` called at line 169, at the TOP of run_bench's per-request logic (before tokenize/readout, after model lookup/cache), in addition to the pre-existing readout/generate reset at line 180 — matches the developer's claimed cross-request-contamination fix, correctly positioned at the request boundary, not just between the two pipeline calls"
  - "Code review, actor/channel architecture: `AppState` (line 54-57) holds only `mpsc::Sender<WorkerJob>` (Send+Sync+Clone); `AppState::new()` (line 60-70) spawns ONE dedicated `std::thread` that owns `HashMap<String, Engine>` locally and loops on `rx.blocking_recv()`; `bench_handler` (line 196-210) is `async`, never touches `Engine`, only does `.send(WorkerJob).await` + `.await`s a `oneshot` reply -- confirmed no inline blocking call in the async handler path, and the single worker thread gives full request serialization (same effective guarantee D_3 required from Mutex+spawn_blocking). Doc comment at line 42-53 gives a concrete technical reason (`Engine` wraps raw FFI pointers -> `!Send` -> `Arc<Mutex<HashMap<String,Engine>>>` cannot satisfy spawn_blocking's `F: Send` bound) for the pivot away from D_3's literal `Arc<Mutex<...>>` instruction; I did not independently re-open `phase-05-http-api-serve.md` to re-verify the 'documented fallback' framing verbatim, but the architecture itself is sound, self-consistent, and empirically proven correct by the concurrency test above"
  - "Lazy-load+cache confirmed empirically: first request to a model pays model_load_ms>0/warmup_ms>0 (apps/cli comparison run: model_load_ms=934, warmup_ms=82); all subsequent requests to the same already-cached model in this session showed model_load_ms=0/warmup_ms=0 (both /bench calls in VR2 and the concurrency test) -- matches `engines.contains_key` branch in app.rs:141-160"
  - "No new `unsafe` in apps/server: `grep -rn unsafe apps/server` -> no matches in app.rs or main.rs"
  - "Server process stability: `ps -p 56718 -o pid,etime,cmd` on server -> single continuous process since 00:23, one process, one `listening on 0.0.0.0:80` line in `logs/server.log` (grep -c = 1, no restart-loop evidence), zero panic/thread-panicked lines in the log; process survived VR2+VR3+VR4 (5 real HTTP requests including the invalid-model error path) without dying"
  - "Preservation: `cargo build --workspace` (debug) on server -> exit clean, only pre-existing warnings (deprecated `Special`, unread `backend` field -- same as E_2). `cargo build --workspace --release` -> exit 0. `cargo test --workspace` -> 3/3 pass (all in pipeline), 0 failures, rest 0 tests -- matches E_2 baseline exactly, no regression"
  - "Preservation, apps/cli still correct: independently re-ran `openjev-cli` with the SAME prompt/options as the VR2 server test -> identical top-level JSON shape (model/prompt/options/timings{7 fields}/readout{probs,best_option}/generate{raw_text,parsed,valid,tokens_generated}) AND near-identical values (readout.probs matches to several decimal places, generate.parsed.answer=billing, tokens_generated=190 -- exact match to the server's response) -- apps/cli and apps/server agree on field shape and semantics, satisfying D_3's DTO-parity requirement without a duplicated/drifted BenchReport definition (BenchReport in app.rs reuses `pipeline::{ReadoutResult,GenerateResult}` and `timing::Timings` directly, does not redefine them)"
  - "Preservation, other structural constraints: workspace `Cargo.toml` members = exactly [apps/cli, apps/server, crates/engine, crates/models, crates/pipeline, crates/timing] (6 crates); `Timings` struct still exactly 7 fields + `#[derive(Serialize,...)]`; all 4 model cache dirs present in `~/.cache/huggingface/hub/`; `ggmlc-run` binary present, executable, at `/tmp/ggmlc/build/runtime/ggmlc-run`; `apps/cli/src/main.rs` still calls `reset_context()` at line 81 (before readout) and line 97 (between readout/generate) -- unchanged from E_2"
  - "G3/G4/G5 preserved: `crates/engine`/`crates/models` source not touched this loop (apps/server only imports their existing public API -- `Engine::load`, `EngineConfig`, `engine.tokenize`/`decode_prompt`/`reset_context`); no new guessed API usage introduced"
unresolved_gaps:
  - "G12 (new): apps/server/src/{app,main}.rs has ZERO committed unit tests (`grep '#\\[test\\]'` -> no matches). All D_3 Validation Requirements are black-box HTTP-level and ARE met (see Verified Behaviors), so this does not block Loop 3 acceptance, but `run_bench`'s branch logic (cache-hit vs cache-miss, the two reset_context placements, ApiError conversions) has no in-repo regression protection -- same pattern as G10 for pipeline's generate.rs"
  - "G13 (new): worker thread has no panic supervision. `AppState::new()` spawns one plain `std::thread` with no `catch_unwind`/supervisor/restart-on-panic. If `run_bench` ever panics (not observed in this loop's testing -- the `.expect(\"just inserted or present\")` at app.rs:162 is logically unreachable given the surrounding if/else, but is still a live `panic!` path in the code), the thread dies silently, the `mpsc::Receiver` drops, and every subsequent `/bench` request would get HTTP 500 \"engine worker thread unavailable\" until the whole process is restarted -- no self-healing. Architectural single-point-of-failure risk, not an observed defect"
  - "G14 (new, risk note): port 80 instead of D_3's example port 8080. D_3 only suggested 8080 (\"e.g. 8080\", not mandatory) and Task 2 required 'confirm nothing else already uses it' -- both satisfied for port 80 (`ss -ltnp` shows only openjev-server on :80). Accepted engineering deviation given the documented external-firewall blocker (VM has no `ufw`/`iptables` -- confirmed: both commands not found on server -- so the block is an external cloud security-group the operator can't self-service; port 80 was verified reachable via a `python3 -m http.server 80` smoke test before binding the real app, per the task context). Residual risk, non-blocking: (a) binding :80 requires root (already running as root -- works, but is not least-privilege), (b) if any reverse proxy/webserver is added to this VM later, it will collide with :80, (c) combined with explicit no-auth scope, the benchmark endpoint sits on the most commonly-probed port on the public internet -- marginally larger unsolicited-scan surface than an unusual high port, though not a functional problem given no-auth was already user-approved for this endpoint regardless of port"
  - "G9 unchanged (open, not addressed this loop -- not in D_3 scope): docs/codebase-summary.md:81 still labels `apps/server` **STUB** and describes it only as a placeholder table, even though apps/server is now a real, working, externally-curl-verified HTTP API. Doc drift is now larger than at E_2 (functional gap between code and docs widened, since server functionality materially expanded this loop) -- still a documentation-sync gap, not a functional defect"
regressions: []
schema_valid: true
---

## 1. Verified Behaviors

- **External reachability, `/health` (VR1).** Ran `curl -s -o /dev/null -w "HTTP_STATUS:%{http_code}" --max-time 15 http://103.146.166.46:80/health` directly from this machine's Bash tool (not SSH) -> `HTTP_STATUS:200`.
- **External reachability, `/bench` with a non-France prompt (VR2).** Used scenario 1 from `researcher-05-benchmark-usecases.md` (email routing: refund/double-charge complaint, options `billing/technical-support/account-access/spam`), run as a real `curl -X POST` from this machine:
  ```
  {"model":"qwen3-0.6b", ..., "timings":{"model_load_ms":0,"warmup_ms":0,"tokenize_ms":0,
   "constrained_readout_ms":112,"generation_ms":2657,"laya_model_load_ms":0,"laya_inference_ms":0},
   "readout":{"probs":{"spam":0.0000109,"billing":0.9998882,"technical-support":0.0000023,
   "account-access":0.0000985},"best_option":"billing"},
   "generate":{"raw_text":"{\"answer\": \"billing\"}","parsed":{"answer":"billing"},
   "valid":true,"tokens_generated":190}}
  ```
  HTTP 200. All 7 `Timings` fields present. `readout.probs` sums to ~1. Both readout and generate agree on the (obviously correct) answer `billing`.
- **Concurrency (VR3).** Fired 2 POST /bench requests near-simultaneously (bash `&` + `wait`) using scenario 5 (incident severity, `sev1-4`) and scenario 6 (content moderation, `allow/flag-for-review/remove/ban-user`) from the same research doc. Both returned HTTP 200 with their own distinct, correct-shaped results (A: `readout.best_option=sev1` [uniform 0.25 each -- readout genuinely uncertain on an ordered-severity prompt forced into flat choice, not a bug], `generate.parsed.answer=sev4`; B: `readout.best_option=flag-for-review` (0.569), `generate.parsed.answer=flag-for-review`). No result-mixing, no crash. Server was still alive and correct on all subsequent calls.
- **Invalid model (VR4).** `POST /bench {"model":"nonexistent-model-xyz",...}` -> HTTP 400, body `{"error":"unknown model: nonexistent-model-xyz"}`. Re-curled `/health` immediately after -> 200. Server did not crash or need a restart.
- **`reset_context` placement (white-box).** Read `apps/server/src/app.rs` directly (not from Developer's claim): line 169 calls `engine.reset_context()` immediately after the model-load/cache branch and BEFORE tokenize (line 172) / readout (line 176) -- this is the new cross-request-isolation fix the task description flagged, and it's at the correct place (top of the per-request path, not just between readout/generate). Line 180 retains the original readout->generate reset. Both are present and correctly ordered.
- **Actor/channel architecture (white-box).** `AppState` (line 54-57) = `{ tx: mpsc::Sender<WorkerJob> }` only. `AppState::new()` (line 60-70) spawns exactly one `std::thread` that owns `HashMap<String, Engine>` for its entire life and processes jobs via `rx.blocking_recv()` in a loop -- `Engine::load` (inside `run_bench`) only ever executes on this thread, so `Engine` (which the code comments document as `!Send` because it wraps raw `llama-cpp-2`/FFI pointers) never crosses a thread boundary. `bench_handler` (line 196-210) is `async fn`, sends a `WorkerJob` + `.await`s a `oneshot::Receiver` -- no inline blocking call in the async path. One worker thread = full request serialization, same guarantee D_3's `Arc<Mutex<...>>` design would have given. This matches all three properties the task asked me to check: (a) never blocks inline in the async handler, (b) full request serialization, (c) lazy-load+cache (confirmed via the `model_load_ms`/`warmup_ms` = 0-on-cache-hit pattern observed across my test responses). I did not re-open `phase-05-http-api-serve.md` myself to re-verify the "documented fallback" framing verbatim -- flagging this as unverified-by-me, though it doesn't change the architecture's soundness, which I verified independently from the code + behavior.
- **No new `unsafe`.** `grep -rn unsafe` over `apps/server/src` -> no matches.
- **Process stability.** `ps -p 56718 -o pid,etime,cmd` -> one process, started 00:23, alive through all of my testing. `logs/server.log`: exactly one `listening on 0.0.0.0:80` line (`grep -c` = 1 -- no restart-loop), zero panic lines. Process absorbed 5 real requests from this loop's QA testing (2 sequential + 2 concurrent + 1 invalid-model) without dying. Note: observed uptime at check time was short (~5 min) since the server was evidently (re)started recently to move from :8080 to :80 -- I can't rule out an earlier crash-restart before that point from the log alone, but the log shows no crash *during* the port-80 run, and the server was stable through every test I threw at it.
- **Preservation -- build/test.** `cargo build --workspace` (debug) exit clean (pre-existing warnings only, same as E_2). `cargo build --workspace --release` exit 0. `cargo test --workspace` -> 3/3 pass (pipeline), 0 failures -- identical to E_2's baseline, no regression.
- **Preservation -- `apps/cli` DTO parity.** Re-ran `openjev-cli` with the exact same prompt/options as the VR2 server test: identical JSON top-level shape, `readout.probs` matches to several decimal places, `generate.parsed.answer=billing`, `tokens_generated=190` -- exact match to the server's response. `BenchReport` in `app.rs` reuses `pipeline::{ReadoutResult, GenerateResult}` + `timing::Timings` directly rather than redefining them, so there's no risk of the two surfaces silently drifting apart.
- **Preservation -- structural.** Workspace `Cargo.toml` members = 6 crates unchanged (`apps/cli, apps/server, crates/engine, crates/models, crates/pipeline, crates/timing`). `Timings` still 7 fields + `Serialize`. All 4 model cache dirs present. `ggmlc-run` present+executable. `apps/cli/src/main.rs` `reset_context` calls unchanged (lines 81, 97). `crates/engine`/`crates/models` source untouched this loop -- G3/G4/G5 not re-opened.

## 2. Unresolved Gaps

- **G12 (new).** Zero committed unit tests in `apps/server` (`app.rs`/`main.rs`). All D_3 Validation Requirements are black-box/HTTP-level and are met, so this does not block acceptance, but `run_bench`'s cache-hit/miss branching, the two `reset_context` call sites, and `ApiError` conversions have no in-repo regression protection. Mirrors G10's pattern (pipeline's `generate.rs`).
- **G13 (new).** No panic supervision on the dedicated worker thread (`AppState::new()`, `app.rs:60-70`). A panic inside `run_bench` (not observed this loop, but `app.rs:162`'s `.expect("just inserted or present")` is a live panic path even if currently unreachable by construction) would silently kill the thread; every subsequent `/bench` call would then fail with 500 "engine worker thread unavailable" until the whole process is restarted -- no self-healing/retry/respawn. Single-point-of-failure risk to track, not a defect observed in this loop's testing.
- **G14 (new, risk note, non-blocking).** Port 80 instead of D_3's example (8080) -- an accepted deviation given the external-firewall blocker documented in the task context (confirmed: `ufw`/`iptables` both "command not found" on the VM, so the block is an external cloud security group the operator can't self-service inside the VM; port 80 reachability was smoke-tested via `python3 -m http.server 80` before binding the real app). D_3's Task 2 only required "confirm nothing else on the server already uses it" (`ss -ltnp` confirms only `openjev-server` on :80) -- satisfied. Residual, non-blocking risks: requires root to bind (already running as root, works but not least-privilege); collides with any future reverse-proxy/webserver on this VM; combined with the explicit no-auth scope, sits on the most commonly-probed public port. None of these are functional defects against D_3's stated requirements.
- **G9 (carried, unchanged).** `docs/codebase-summary.md:81` still marks `apps/server` **STUB**; the gap is now wider than at E_2 since apps/server materially gained real, externally-verified functionality this loop without a doc update. Not in D_3's scope (not this loop's focus per D_3 Out-of-scope), but the drift is growing.

## 3. Regressions

None. All Loop 1/2 preservation constraints (workspace structure, `Timings` shape, cached models, `ggmlc-run`, build/test, `reset_context` ordering in `apps/cli`, G3/G4/G5 closure) re-verified and hold. `apps/cli` output is unchanged and matches `apps/server`'s output for the same input.

## 4. Runtime Check

- External curl (from this Windows machine, NOT SSH):
  - `GET http://103.146.166.46:80/health` -> 200.
  - `POST http://103.146.166.46:80/bench` (email-routing scenario) -> 200, valid `BenchReport`, correct classification.
  - 2 concurrent `POST /bench` (severity + moderation scenarios) -> both 200, correct, no crash/mix.
  - `POST /bench` invalid model -> 400 JSON error, not 500/panic; `/health` re-check after -> 200.
- SSH-side (`root@103.146.166.46`, `/tmp/openjev`):
  - `ps -p 56718 -o pid,etime,cmd` -> single continuous `openjev-server --port 80` process since 00:23, no restart-loop in log, zero panics logged.
  - `ss -ltnp | grep :80` -> only `openjev-server` bound.
  - `cargo build --workspace` (debug) -> exit 0, pre-existing warnings only.
  - `cargo build --workspace --release` -> exit 0.
  - `cargo test --workspace` -> 3/3 pass (pipeline), 0 failures.
  - `openjev-cli` re-run, same prompt/options as the server VR2 test -> matching field shape and near-identical values (exact `tokens_generated=190` match).
  - Structural checks (workspace members, `Timings` fields, model cache dirs, `ggmlc-run` binary, `apps/cli` `reset_context` call sites) all confirmed unchanged.
- `schema_valid: true` -- E_3 produced against a workspace that built, tested, and served real external HTTP traffic successfully on the required Linux server target.

## 5. Assessment

**Accept: yes.** All 4 of D_3's Validation Requirements are independently verified via real `curl` from this machine's own shell (never from inside the SSH session), including the two harder ones (concurrency, invalid-model 4xx). The actor/channel pivot away from the literal `Arc<Mutex<...>>` instruction is well-justified (real `!Send` compile error from FFI raw pointers), architecturally sound (verified from source: single worker thread, no inline blocking in the async handler, full serialization), and empirically proven by the concurrency test producing two distinct, correct, non-corrupted results. The new cross-request `reset_context()` at the top of `run_bench` is real, present, and correctly placed. No unsafe code was added. All preservation constraints from D_3's frontmatter hold: build/test on the server, `apps/cli` correctness and DTO-shape parity with `apps/server`, 6-crate structure, `Timings` 7 fields, 4 cached models, `ggmlc-run` binary, and G3/G4/G5 closure.

Three non-blocking items carried forward as new gaps: G12 (no unit tests in `apps/server` -- coverage gap, not a functional failure, since all VRs are black-box and pass), G13 (worker-thread panic has no supervision/recovery -- architectural SPOF risk, not observed this loop), and G14 (port 80 vs 8080 -- accepted, reasoned deviation with minor residual risk, not a functional defect). G9 (stale docs) persists and widens but was out of scope this loop.

**Port-80 deviation verdict: acceptable.** D_3 said "e.g. 8080" (example, not mandate); Task 2's actual requirement ("confirm nothing else uses the port") was met for :80; the external-firewall diagnosis is corroborated independently (`ufw`/`iptables` genuinely absent from the VM, consistent with an external cloud security-group block the operator can't self-service). Not a regression, not a deviation from any hard requirement.

## Unresolved Questions

- Should G13 (worker-thread panic supervision) be a Loop 4 task, or deferred until concurrency/load is stress-tested more (current evidence is only 2 concurrent requests, not a real load test)? Needs a planner call on priority vs. Laya/other-models work.
- Should the operator pursue getting :8080 (or another non-privileged, less-commonly-probed port) opened via the actual cloud security group, to avoid the root-bind-on-:80 and reverse-proxy-collision risks in G14 long-term? Infra decision, not a QA one.
- Is G12 (apps/server unit tests) worth a small dedicated task, or does black-box HTTP-level D_3 validation (now passing) count as sufficient coverage for this surface going forward, same open question style as G10?
