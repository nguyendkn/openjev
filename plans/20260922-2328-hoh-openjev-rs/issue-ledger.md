# Issue Ledger — HoH run hoh-openjev-rs

| ID | Loop opened | Behavior | Status | Loop closed | Reopened | Evidence |
|----|-------------|----------|--------|-------------|----------|----------|
| G1 | 1 | Windows `cargo build --workspace` fails: libclang missing for `llama-cpp-sys-2` bindgen | open | — | — | loop-01-evidence.md § Runtime Check |
| G2 | 1 | Unresolved #1/#2 (model repo IDs): fallback IDs work but openjev GitHub source never cited | open | — | — | loop-01-evidence.md § Unresolved Gaps |
| G3 | 1 | Unresolved #3 (hf-hub API): stub only, no real API usage/confirmation | closed | 2 | — | loop-02-evidence.md § Verified Behaviors (G3); crates/models/src/download.rs real HFClientSync/download_file usage, grep-confirmed against hf-hub-1.0.0 source |
| G4 | 1 | Unresolved #4 (llama-cpp-2 logits/KV-cache API): inferred, not confirmed against source/cargo doc | closed | 2 | — | loop-02-evidence.md § Verified Behaviors (G4); crates/engine/src/lib.rs real llama-cpp-2 0.1.156 usage, grep-confirmed exact signatures against resolved source |
| G5 | 1 | Unresolved #5 (model licensing): pending, no per-repo HF license field checked | closed | 2 | — | loop-02-evidence.md § Verified Behaviors (G5); curl HF API for all 4 repos, cardData.license=apache-2.0 + sha matches pinned revision, all 4 |
| G6 | 1 | Unresolved #6 (ggmlc-run typed-decision tensor-binding semantics for Laya): tensor names known, encoding/output semantics unconfirmed | closed | 5 | — | loop-05-evidence.md § Verified Behaviors; note: approach changed from the generic `ggmlc-run` binary (never resolved) to the dedicated `laya` binary's `laya serve` + `/v1/decide` REST API (Loop 5's corrected understanding) — QA independently curled a live `/v1/decide` response and confirmed `crates/models/src/laya.rs`'s `AnswerEntry` parsing (`choice`/`probabilities`/`confidence`) matches it field-for-field. The underlying uncertainty (how to get real Laya scoring) is resolved via this different, verified mechanism, not via the original `ggmlc-run` tensor-binding path |
| G7 | 1 | Unresolved #7 (ggmlc-run Windows build): never attempted | open | — | — | loop-01-evidence.md § Unresolved Gaps — still out of scope Loop 5 (D_5 Out-of-scope: "Windows build (G7/G1) — still descoped, Linux is authoritative"); NOTE — do not conflate with the separately-achieved Linux `laya` binary build this loop (see G6/G8), which is a different binary on a different OS and does not close this Windows-specific gap |
| G8 | 1 | Unresolved #8 (Laya CPU latency sanity check): not measured | closed | 5 | — | loop-05-evidence.md § Verified Behaviors; warm `laya serve` HTTP round-trip independently measured live multiple times this loop (`laya_inference_ms` ~8000-9000ms per call, external `/bench` and SSH curl both), first real CPU latency numbers for Laya (previously only GPU numbers existed per D_5). Cold `laya decide` CLI figure (~39.6s) per README, not independently re-timed by QA this loop — see loop-05-evidence.md Unresolved Questions |
| G9 | 2 | docs/codebase-summary.md not updated for Loop 2: engine/models still marked [STUB], llama-cpp-2/hf-hub items still marked UNVERIFIED/Inferred despite real implementation now present | closed | 4 | — | loop-04-evidence.md § Verified Behaviors (G9); docs/codebase-summary.md + docs/project-overview-pdr.md Per-Crate State/Open Risks sections re-read in full, content substantively accurate (see G16 for one residual cosmetic inconsistency found) |
| G10 | 2 | crates/pipeline/src/generate.rs strip_think/parse_and_validate have no committed unit tests (only constrained_softmax in readout.rs does); "never panic" claim verified only via QA's uncommitted ad hoc tests | open | — | — | loop-02-evidence.md § Unresolved Gaps; § Verified Behaviors (15 temp adversarial tests) |
| G11 | 2 | crates/engine/src/lib.rs Engine::load unsafe lifetime-widening (transmute to 'static over Box<LlamaModel>) is correct today but not compiler-enforced — risk if struct fields reordered or model taken by value in a future change | open | — | — | loop-02-evidence.md § Unresolved Gaps; crates/engine/src/lib.rs:69-75 |
| G12 | 3 | apps/server/src/{app,main}.rs has zero committed unit tests — run_bench cache-hit/miss branching, dual reset_context placements, ApiError conversions have no in-repo regression protection (all VR coverage is black-box HTTP only) | open | — | — | loop-03-evidence.md § Unresolved Gaps |
| G13 | 3 | apps/server worker thread (AppState::new, app.rs:60-70) has no panic supervision/restart — a panic in run_bench would silently kill the thread and all subsequent /bench calls would 500 until full process restart; not observed this loop but a live architectural SPOF | closed | 5 | — | loop-04-evidence.md § Verified Behaviors (G13 fault-injection test, `supervisor_loop`/`worker_loop` respawn-with-empty-cache fix, first proven working) — administrative note: fix was verified working in Loop 4 but this ledger row was never updated to closed at the time; loop-05-evidence.md independently re-proves G13 still works via a fresh, separate fault injection (the G15 re-verification test doubles as a G13 regression check: panic → clean 500 → `/health` stays 200 → next request self-heals with `model_load_ms>0` fresh reload) |
| G14 | 3 | apps/server bound to port 80 instead of D_3's example port 8080, due to external cloud firewall blocking 8080 (no ufw/iptables in VM to self-service) — accepted deviation, D_3 only suggested 8080 as example; residual non-blocking risk: root-required bind, future reverse-proxy collision, most-probed public port combined with no-auth scope | open | — | — | loop-03-evidence.md § Unresolved Gaps; § Assessment (port-80 deviation verdict: acceptable) |
| G15 | 4 | apps/server/src/app.rs:108 `panic_message(&payload)` bug (not cosmetic): passes `&Box<dyn Any+Send>` instead of `&*payload`, so `downcast_ref::<&str>()`/`downcast_ref::<String>()` ALWAYS return None regardless of real payload type — every worker-thread panic is logged as `<non-string panic payload>` instead of its real message. Independently reproduced in 3 isolated minimal Rust programs on the same server/toolchain; confirmed live via a self-constructed fault-injection panic whose real message (a plain `&str` literal, confirmed via the default panic hook's own stderr) was still logged as non-string by `supervisor_loop`'s own eprintln. Does not affect respawn/recovery correctness (verified separately, works), but breaks production debuggability of every future panic. Fix: `panic_message(&*payload)` | closed | 5 | — | loop-05-evidence.md § Verified Behaviors (G15 re-verification); source confirmed `panic_message(&*payload)` now at app.rs:114, and a fresh, independent fault-injection (separate from Loop 4's) proves the real panic message now logs correctly instead of `<non-string panic payload>`; fault-injection code fully reverted, md5sum-verified clean |
| G16 | 4 | docs/codebase-summary.md self-contradicts: Directory Tree ASCII diagram (lines 16-18) still labels models/engine/pipeline `[STUB]` while the Per-Crate State section in the same file (lines 42+) correctly says IMPLEMENTED — leftover from the G9 refresh, prose updated but summary tree missed. Cosmetic only | open | — | — | loop-04-evidence.md § Unresolved Gaps (G16); confirmed still unfixed in Loop 5 via direct re-read, loop-05-evidence.md § Unresolved Gaps (G16) |
| G17 | 5 | `laya serve` binds `0.0.0.0:8090`, not `127.0.0.1:8090` as D_5's task text specifies and as README.md:64 explicitly (and incorrectly) claims ("bound to `127.0.0.1:8090`... not externally reachable by design"). `ss -ltnp` confirms `0.0.0.0:8090` both before and after a restart this session. Root cause: the `laya` binary's `serve` subcommand has no `--host`/`--bind` flag at all (confirmed via `laya help`) — a binary limitation, not a config mistake. Currently not externally reachable in practice (external curl timed out after 8s, unlike port 80 which answers normally) — but that protection is a cloud-level firewall/security-group rule external to this repo and unverifiable from inside the VM (no `iptables`/`ufw` present at all). Laya's own `serve` help text states auth is off by default; if the external filter is ever loosened, `/v1/decide` becomes a fully unauthenticated, unrestricted internet-facing inference endpoint (DoS/resource-abuse/model-probing exposure), which is exactly the scenario D_5's architecture decision explicitly intended to rule out. README's "by design" wording should be corrected to describe the real (external, unverified) protection mechanism | closed | 6 | — | loop-05-evidence.md § Unresolved Gaps (G17); loop-06-evidence.md § Verified Behaviors (G17 mitigation independently verified from an external vantage point: `iptables -L INPUT -n -v` shows a real, traffic-exercised loopback-only ACCEPT + catch-all DROP for :8090, external curl from QA's own machine timed out, internal Laya calls unaffected; README.md's false claim corrected) — residual reboot-persistence risk tracked separately as G18 |
| G18 | 6 | Loop 6's `iptables` mitigation for G17 (loopback-only :8090) is not persisted across a reboot — no `iptables-persistent`/`netfilter-persistent` installed. If the box reboots, :8090 protection reverts to the unverified external/cloud-level firewall G17 originally found. Low-severity, already an acknowledged caveat in the Developer's own report (not a QA-discovered oversight); cheap Loop 7+ follow-up | closed | 7 | — | loop-07-dev-doc.md Task 6 / server-tuning-results.md §7 (Developer: installed+enabled `openjev-laya-firewall.service`, oneshot systemd unit reapplying the 2 iptables rules, verified via a live rule-removal+restart simulation); loop-07-evidence.md § Verified Behaviors (QA independently re-verified `systemctl status`/`is-enabled`, read the unit file directly, and ran QA's OWN rule-removal+marker-removal+`systemctl restart` simulation separate from the Developer's — rules and marker both correctly reapplied, G17's external-block intact afterward) |
| G19 | 8 | D_8 Task 1 explicitly listed `docs/project-overview-pdr.md` and `docs/codebase-summary.md` as places to check/update the `laya serve` startup command + add `--threads 28`; only `README.md` was actually updated. Both files still contain zero mentions of Loop 6/7/8 at all and still cite the Loop-5-era Laya latency figure (~7.5-8.9s per call, the undiscovered threads=4 bug's own number) as current — now ~5-6x stale/misleading vs the real ~1.2-1.4s post-fix number. Cosmetic (doesn't affect running config), but a real doc-staleness gap the loop's own task list called out and didn't fully close | open | — | — | loop-08-evidence.md § Unresolved Gaps; grep for "Loop 8"/"Loop 7"/"Loop 6" in both files on the server returns 0 matches, confirmed directly |
| G20 | 8 | `README.md`'s "Laya Server Process" section (line 71) still states the G17 `iptables` rule "is NOT persisted across a reboot ... re-apply it after any server reboot, or install persistence" — this directly contradicts G18 (closed Loop 7), which installed exactly that persistence via `openjev-laya-firewall.service`. Loop 8 edited the very next paragraph in the same README section (the new "Process lifecycle — systemd-managed" paragraph, line 77+) without correcting this pre-existing stale sentence directly above it. Cosmetic, non-blocking | open | — | — | loop-08-evidence.md § Unresolved Gaps; README.md line 71 vs G18 row (closed loop 7) and loop-07-evidence.md § Verified Behaviors |
