### 4.B Consistency — 4 BLOCKER / 1 MINOR

#### §4.B.1 — plan.md phase table ↔ phase-*.md agree on names/order/scope
PASS. `plan.md:94-102` Phases table and each `phase-0N-*.md` frontmatter (`name`, `depends_on`) match 1:1 (phase-00 `depends_on: []`; phase-01 `[0]`; phase-02 `[1]`; phase-03 `[1]`; phase-04 `[2,3]`; phase-05 `[2,3]`; phase-06 `[1,2,3,4,5]`). No drift.

#### §4.B.2 — Interfaces/types used across phases defined consistently
2 BLOCKERs.

- **BLOCKER — `Timings` type consumed before it is defined.** `phase-02-pipeline-constrained-readout.md:55` (`pub fn run_readout(engine: &mut Engine, prompt: &str, options: &[String], timings: &mut Timings) -> Result<ReadoutResult, PipelineError>`) and `phase-03-pipeline-generation.md:61` (`pub fn run_generate(..., timings: &mut Timings) -> ...`) both take `&mut Timings` as a parameter. `Timings` is not defined until `phase-04-cli-bench.md:53-55` (`src/timing.rs`), and Phase 4 `depends_on: [2, 3]` (`phase-04-cli-bench.md:5`). Phases 2 and 3 cannot compile against a struct owned by a phase that depends on them finishing first. Phase 2's own Overview even flags this as unresolved ("this phase's function signature should return timing-relevant `Instant` markers or accept a `&mut Timings`... see Phase 4/5 for where `Timings` itself is assembled" — `phase-02-pipeline-constrained-readout.md:40-44`) but the Architecture section still commits to the `&mut Timings` signature anyway.

- **BLOCKER — Phase 3 needs an `Engine` capability Phase 1 never defines.** Phase 1's committed `Engine` API surface (`phase-01-engine-model-management.md:63-69`) exposes only `load`/`tokenize`/`decode_prompt`/`decode_next`/`warmup` — no sampling method. R1's confirmed generation-loop pattern samples directly off the live `LlamaContext` (`sampler.sample(&ctx, ...)`, `researcher-01-llama-cpp-2-engine.md:56-62`), and `Engine.ctx` is a private field only reachable from within the `engine` module. Phase 3 explicitly acknowledges the gap and proposes bolting on `Engine::sample_greedy` in `src/engine/mod.rs` "if Phase 1 already shipped before this is written" (`phase-03-pipeline-generation.md:86-90`) — but that file is never listed in Phase 3's own "Related Code Files" section (`phase-03-pipeline-generation.md:64-72`, which lists only `pipeline/generate.rs`, `pipeline/mod.rs`, `pipeline/error.rs`), and `src/engine/**` is exclusively owned by Phase 1 per `plan.md:115`. The interface Phase 3 depends on is undefined in the architecture it's supposed to build on.

#### §4.B.3 — Research findings reflected, nothing contradicted/ignored
1 MINOR.

- **MINOR — KV-cache reset between the two pipelines sharing one `Engine` is never resolved.** R1 flags `kv_cache_clear`/`kv_cache_seq_rm` as unverified and needed "to confirm cache-reuse/reset behavior between independent benchmark runs" (`researcher-01-llama-cpp-2-engine.md:48-50,124`); Phase 0 lists the KV-cache reset call as one of the facts to close (`phase-00-spike-verification.md:34-35`). But Phase 1's `Engine` (`phase-01-engine-model-management.md:63-69`) exposes no reset method, and Phase 4's bench flow calls `run_readout` then `run_generate` back-to-back on the same warmed `Engine` instance (`phase-04-cli-bench.md:87-88`) with no mention of clearing context/KV-cache state between them. Not a stated contradiction, but a closed-loop gap: the fact Phase 0 promises to resolve is never consumed downstream.

#### §4.B.4 — No internal contradiction across artifacts (ownership/order/schema)
2 BLOCKERs, both on the "no overlap" claim in `plan.md`'s Parallel Execution Matrix.

- **BLOCKER — Phase 2/3 `src/pipeline/mod.rs` overlap contradicts plan.md's "no overlap" claim.** `plan.md:116-117` assigns Phase 2 the exclusive glob `src/pipeline/readout.rs` and Phase 3 the exclusive glob `src/pipeline/generate.rs`, then `plan.md:122-124` states "Phases 2/3 touch disjoint files (no overlap). ... Safe to dispatch each wave's phases concurrently." In fact both phase files modify `src/pipeline/mod.rs`: Phase 2 creates it (`phase-02-pipeline-constrained-readout.md:61-62`) and Phase 3 modifies it (`phase-03-pipeline-generation.md:67-70`, which even acknowledges the coordination risk itself). The phase files handle the mechanics sanely (each appends its own `pub mod` line), but plan.md's stated ownership/no-overlap guarantee is factually wrong and should list `src/pipeline/mod.rs` as shared.

- **BLOCKER — Phase 4/5 `src/cli/**` overlap contradicts plan.md's "no overlap" claim.** `plan.md:118-119` assigns `src/cli/**` exclusively to Phase 4 and `src/server/**` exclusively to Phase 5, then `plan.md:122-124` repeats "Phases 4/5 touch disjoint files (no overlap)." But Phase 5 creates `src/cli/serve.rs` and modifies `src/cli/mod.rs` (`phase-05-http-api-serve.md:67-69`) — both under the `src/cli/**` glob plan.md assigns solely to Phase 4. Phase 4's own file acknowledges Phase 5 will touch the `Command` enum (`phase-04-cli-bench.md:68-70`, `phase-04-cli-bench.md:73`), so the phase files themselves are internally aware, but plan.md's ownership matrix/no-overlap statement is inconsistent with what the phase files actually do.
