### 4.D Testability — 1 BLOCKER / 2 MINOR

**Commands/checks performed:**
- Read `plan.md` (frontmatter `lane: normal`, confirmed).
- Read `workflows/quality-gate.md` in full (§5 coverage thresholds = line ≥90% AND branch ≥75%; §6 plan-author requirements; §8 lane exemption = `tiny` only, no exemption for `normal`).
- Read all 7 phase files in full: `phase-00-spike-verification.md`, `phase-01-engine-model-management.md`, `phase-02-pipeline-constrained-readout.md`, `phase-03-pipeline-generation.md`, `phase-04-cli-bench.md`, `phase-05-http-api-serve.md`, `phase-06-test-suite-ci.md`.
- Grep `pattern: "features (integration|native|real-model)|feature = \"integration\""` across the plan dir — confirmed 1,2,4,5,6 all use `--features integration` / `feature = "integration"`; phase-03 was missing from that specific grep (line-wrap artifact), so re-grepped `phase-03-pipeline-generation.md` alone for `integration` and confirmed it also uses `cargo test --features integration pipeline::generate` (plan.md:131-132). **No divergent feature-flag name found anywhere** — all 6 code-bearing phases consistently gate real-model tests behind `integration`.
- Cross-checked each phase's stated coverage target against quality-gate.md §5's actual numbers (90% line / 75% branch).

**Findings:**

1. **BLOCKER** — `phase-00-spike-verification.md:97-104` ("Test Strategy & Quality Gate" section). Lane is `normal` (plan.md:7), so quality-gate.md §8 does NOT exempt this phase — only lane `tiny` is exempt, and no partial exemption exists for `normal`/`high-risk` (§8: "no partial exemption"). Phase 0 self-declares "No unit/integration/e2e tests apply" and provides zero e2e scenario and zero test-runner/coverage-tool evidence command (only `cargo build`). This fails rubric item 2 (e2e scenario missing = BLOCKER) and item 4 (no test-runner+coverage invocation) outright. The phase's own rationale ("this is a spike, no product logic yet") is reasonable engineering judgment but is not an exemption quality-gate.md actually grants — the gate's only carve-out is lane `tiny`. Plan author should either (a) get plan.md to explicitly mark Phase 0 as `lane: tiny` for gate purposes with citation to §8, or (b) add a minimal e2e-equivalent evidence command (e.g. `cargo build` output IS arguably the "e2e" for a build-only skeleton — but this must be stated as such and satisfy §7's evidence requirements explicitly, not asserted as "N/A").

2. **MINOR** — `phase-01-engine-model-management.md:124-128` ("Test Strategy & Quality Gate" section). Unlike Phases 2-5 which each quote a specific named e2e scenario string (e.g. phase-02:107-108 `"given a real biased prompt, the favored option's probability is highest"`), Phase 1's section only says e2e is `"~10%, deferred to Phase 6's consolidated pass"` — no named scenario given here. The Success Criteria section (phase-01:107-112) does describe an equivalent concrete scenario ("loads Qwen3-0.6B end-to-end: download → checksum pass → load → tokenize → decode_prompt → non-empty logits") which would satisfy the bar if copied/named inside the Quality Gate section itself — it currently sits outside it. Not a BLOCKER because the substance exists elsewhere in the same file and downstream phases (4, 6) do name concrete e2e scenarios that exercise this module's code path, but the Quality Gate section itself is technically missing its own named e2e line per rubric item 2's letter.

3. **MINOR** — `phase-00-spike-verification.md` and `phase-06-test-suite-ci.md` acceptance criteria are written as flat `AC: (1)...(2)...` assertion lists, not strict Given/When/Then phrasing (unlike phases 1-5, which use explicit "Given X, When Y, Then Z" wording). Both are still testable/checkable (e.g. phase-06:153-155: "`cargo test` exits 0, no network"), so this is a format nit, not a testability blocker.

**Non-findings (verified correct, no issue):**
- Coverage target: all phases 1-6 state exactly "≥90% line / ≥75% branch," matching quality-gate.md §5 verbatim. No phase understates or overstates the threshold.
- Evidence commands in phases 1-6 are syntactically valid cargo idiom: module-path test filters (`cargo test pipeline::readout`, `cargo test cli::`, `cargo test server::`) are valid substring-match filters for `cargo test`; `--features integration` is consistently the same flag name project-wide (see grep above — no naming inconsistency bug).
- Phases 2, 3, 4, 5 each have a valid G/W/T-format spec and at least one explicitly named e2e scenario (phase-05 has two: real-model POST /bench, and the 2-concurrent-request scenario).
